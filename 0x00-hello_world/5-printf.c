#include <stdio.h>

/**
 * main to print "with proper grammar, but the outcome 
 * is a piece of art, then followed byt a new line."
 *Return: Always 0.
 * 
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
